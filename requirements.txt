cd telegram_support_bot

# Replace requirements.txt
cat > requirements.txt << 'EOF'
python-telegram-bot==20.7
flask==3.0.0
EOF

# Push to trigger Render redeploy
git add requirements.txt
git commit -m "Remove pandas - fix Render build"
git push
